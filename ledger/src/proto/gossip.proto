syntax = "proto3";

package gossip;

service GossipService {
  rpc SendIntent (Intent) returns (Response);
  rpc SendDkg (Dkg) returns (Response);
}

message Intent {
  string asset = 1;
}

message Dkg {
  string msg = 1;
}

message Response {
  string result = 1;
}